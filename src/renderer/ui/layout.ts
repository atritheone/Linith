/**
 * The application shell is intentionally CSS-driven. Keeping this entry point
 * makes the renderer bootstrap stable while avoiding runtime measurements that
 * previously coupled the footer and rules text to the board bounds.
 */
export function initDynamicLayout(): void {
  // Reserved for layout behavior that cannot be expressed with CSS.
}
