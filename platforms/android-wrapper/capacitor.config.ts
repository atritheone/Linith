import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.linith.app",
  appName: "Linith",
  webDir: "www",
  android: {
    path: "android"
  }
};

export default config;
