{
  "name": "linith-android-wrapper",
  "version": "0.232.0",
  "private": true,
  "description": "Android Studio wrapper for the shared Linith renderer build.",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "copy:web": "node scripts/copy-web-assets.mjs",
    "sync": "npm run copy:web && cap sync android",
    "open": "cap open android",
    "apk:debug": "npm run sync && cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "typescript": "^5.9.3"
  },
  "overrides": {
    "xcode": {
      "uuid": "^11.1.1"
    }
  }
}
