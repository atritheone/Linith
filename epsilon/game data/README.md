# Epsilon training data

This directory is for local replay buffers, evaluation batches, and other
NumPy training datasets. Generated `.npz` files are intentionally excluded from
normal Git commits because they are large and change frequently.

Keep durable datasets in versioned object storage and document the source,
checksum, and compatible Epsilon version when sharing them.
