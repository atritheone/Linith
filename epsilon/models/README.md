# Epsilon model checkpoints

This directory is for local Epsilon/AlphaZero model checkpoints. Checkpoints
(`.pt`) are generated training artifacts and are intentionally excluded from
normal Git commits.

Place the checkpoint named by a training command here or pass its path through
the command's model argument. Long-lived published checkpoints should be stored
as versioned release assets or in dedicated model storage rather than ordinary
Git history.
